[user]
	email = patric.berthelsen@gmail.com
	name = patricab
[push]
	default = matching
[core]
	editor = code --wait
[credential]
	helper = store
[alias]
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --branches
	ls = ls-tree -r HEAD --name-only
